---
name: Checks

on: [push]

jobs:
  test-lint:
    name: Test and Lint
    runs-on: ubuntu-20.04
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_user }}
          password: ${{ secrets.DOCKERHUB_token}}
      - name: Checkout
        uses: actions/checkout@v2

      # Add Back Once Tests are complete

      # - name: Test
      #   run: cd server && docker compose run --rm app sh -c "python manage.py wait_for_db && python manage.py test"
      # - name: Lint
      #   run: cd server && docker compose run --rm app sh -c "flake8"